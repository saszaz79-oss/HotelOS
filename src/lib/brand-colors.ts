/** Literal hex equivalents of --primary/--accent (globals.css) — icon.tsx/apple-icon.tsx and the icon-*.png route handlers render via satori, which can't resolve CSS custom properties or hsl(). */
export const BRAND_NAVY = '#1c2b4a';
export const BRAND_GOLD = '#be8c37';
